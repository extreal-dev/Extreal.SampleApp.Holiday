namespace Extreal.SampleApp.Holiday.App
{
    public enum StageName
    {
        // Screen
        TitleScreen = 100,
        AvatarSelectionScreen = 101,

        // Room
        VirtualSpace = 200,
    }
}
