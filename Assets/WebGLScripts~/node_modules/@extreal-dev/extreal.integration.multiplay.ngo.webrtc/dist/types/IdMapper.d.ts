declare class IdMapper {
    private readonly strToNumMapping;
    private readonly numToStrMapping;
    add: (id: string) => void;
    private generate;
    has: (id: string | number) => boolean;
    get: (id: string | number) => string | number | undefined;
    remove: (id: string) => void;
    clear: () => void;
}
export { IdMapper };
